using SomeStuff.Yes;

namespace Test;

static class Program
{
	SomeClass member;

	SomeClass GetMember(SomeClass inClass)
    {
        s = "You know it";
    }

    destroyer Default(SomeClass destroyedClass)
    {
    }
}