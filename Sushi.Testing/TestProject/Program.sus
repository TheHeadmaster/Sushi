using SomeStuff.Yes;

namespace Test;

public static class Program
{
    int32 someInt;

	SomeClass member;

    SomeClass GetMember(SomeClass inClass, SomeClass outClass)
    {
        destroy inClass Default;
        destroy outClass Default;
    }

    creator()
    {
        
    }

    destroyer Default(SomeClass destroyedClass)
    {
        destroy member Default;
        destroy destroyedClass Default;
    }
}
