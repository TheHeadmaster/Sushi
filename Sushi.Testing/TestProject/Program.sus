using SomeStuff.Yes;

namespace Test;

static class Program
{
	SomeClass member;

	SomeClass GetMember(SomeClass inClass)
    {
        s = "You know it";
        create SomeClass();
    }

    creator()
    {
        
    }

    destroyer Default(SomeClass destroyedClass)
    {
        destroy member Default;
    }
}